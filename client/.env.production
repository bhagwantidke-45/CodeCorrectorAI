# ── Production environment variables ──────────────────────────────────────────
# This file is loaded by Vite ONLY during `npm run build` (production builds).
# It is NOT committed to Git if you add *.env to .gitignore.
# ⚠️  Set this to your ACTUAL backend Vercel URL (the server project URL):
VITE_API_URL=https://code-corrector-ai.vercel.app/api
